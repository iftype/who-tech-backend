# 2026-06-07

## 테코톡(TecoTalk) 수집·매칭 기능 + 블로그 내부 조회수

**목표**: 우아한테크코스 테코톡 유튜브 재생목록(689개)을 저장하고, 업로드 연도+발표자 닉네임으로 멤버와 매칭. 주 1회 갱신. 더불어 블로그 글 내부 조회수 저장.

**백엔드**:

- `prisma/schema.prisma`: `TecoTalk` + `TecoTalkSpeaker`(M:N) 모델, `BlogPost.viewCount`(내부 클릭수)
  - 마이그레이션 2개 (수기 작성 후 `migrate deploy` — MissionRepo 드리프트로 인한 reset 회피):
    `..050604_add_tecotalk_and_blogpost_viewcount`, `..053000_tecotalk_multi_speaker`
- `src/features/tecotalk/`
  - `youtube.ts`: YouTube Data API v3 클라이언트 (playlistItems→videos, 조회수 포함, ~28 쿼터)
  - `tecotalk.parser.ts`: `extractSpeakerZone`(발표자 구역) + `nicknameInZone`(닉네임 포함 검사)
  - `tecotalk.service.ts`: 연도→`detectCohort`→기수, 발표자 구역에서 우리 닉네임 포함 매칭. fetchVideos 주입 가능
  - `tecotalk.route.ts`: `POST /admin/tecotalk/sync`, `GET /admin/tecotalk`
- `src/db/repositories/tecotalk.repository.ts`: upsertByVideoId / setSpeakers(M:N) / findByWorkspace / findByMemberId
- 노출: 멤버 상세에 `tecoTalks[]`, 피드 항목에 `id`+`viewCount` 추가
- 블로그 내부 조회수: `incrementViewCount`, `visitBlogPost`, `GET /members/blog-posts/:id/visit`(302)
- `.github/workflows/tecotalk-sync.yml`: 매주 일요일 cron
- 테스트: `tecotalk.parser.test.ts`, `tecotalk.service.test.ts` (단독/2인 공동/미매칭)

**매칭 규칙 (정책)**: 제목에서 추출 X → **우리 닉네임을 제목에 매칭**. 발표자 구역(첫 `의 `/콜론 앞)에서
해당 기수 멤버 닉네임을 포함 검사. `프리, 말론의 ...` 같은 2인 공동 발표는 모두 연결(M:N). 업로드 연도로 기수 판별.

**프론트 (frontend/apps/web)**:

- `types`: `TecoTalk`, `MemberDetail.tecoTalks`, `FeedItem.{id,viewCount}`
- `components/ui/TecoTalkBadge.tsx`: 프로필 헤더 테코톡 뱃지(영상 링크) — 프로필에서만 노출
- `app/[githubId]/MemberDetailClient.tsx`: 뱃지 행 추가
- `features/feed/FeedRow.tsx`: 프로필 사진 밑 눈 아이콘+조회수, 제목 링크를 `/api/members/blog-posts/:id/visit`로 변경(클릭 집계)

**필요 설정**: 서버 환경변수 `YOUTUBE_API_KEY` (선택 `TECOTALK_PLAYLIST_ID`). 키는 서버 IP(168.107.51.150)로 제한됨.

**검증**: 백엔드 typecheck 0 / unit 93 pass / integration 12 pass(`--runInBand`). 프론트 typecheck 0 / lint 0.
파서는 실제 재생목록 제목 100개로 검증(100/100 파싱, 오탐 0).

## 인수인계

- `YOUTUBE_API_KEY`를 서버 `.env` 및 PM2 환경(`pm2 reload backend --update-env`)에 주입해야 sync 동작 (키는 서버 IP로 제한됨 → 로컬 sync 불가, 서버에서 검증)
- 최초 1회 `POST /admin/tecotalk/sync` 수동 실행으로 백필 후 cron이 주간 갱신
- 매칭 정확도는 운영 후 `GET /admin/tecotalk?matchStatus=ambiguous|unmatched` 보고 닉네임 경계 규칙 보강
- 프론트는 backend 배포 후 `tecoTalks`/`viewCount` 응답 필드에 의존 → 백엔드 먼저 배포 권장
- (참고) 통합테스트는 dev.db 공유로 병렬 시 깨짐 → `--runInBand`
